# Feature extractor trust registry

> **Living document.** The single source of truth for which feature columns the
> trustworthy/experimental analysis may use. `src/swb_extract/registry.py` **parses
> this file**, and the loader (`load_features_table`) enforces it. To change a
> feature's trust, **move its row between sections** — that is the whole workflow.
>
> **Buckets** (the section a row lives under = its status):
> - **Trusted** — correctness confirmed; analysis may rely on it.
> - **WIP** — built and producing values, but not yet confirmed. Usable in
>   experiments; the loader prints a warning naming every WIP column it hands you.
> - **Deprecated** — do not use. Kept for paper replication only.
>
> Promote WIP → Trusted only after the extractor is confirmed (unit tests + a
> distribution sanity check + ideally the NXT-gold check, audit §4C). See
> `docs/PIPELINE.md` for the add-a-feature and recompute procedure.
>
> _Last reviewed: 2026-07-09 (NB07 Step 14, submission plan T6 — the two overlap-split
> columns added as WIP pre-rebuild and promoted to Trusted on the pre-registered gold
> checks; earlier same day: Step 11 trust adjudication, 26 rows promoted, per-utterance
> Personal Focus Score deprecated). 2026-08-19: loudness word-tight redesign — re-extracted
> corpus-wide, sanity-gated same day, all 4 columns Trusted (+1 new diagnostic).
> Counts: 46 Trusted · 27 WIP · 8 Deprecated (2026-09-12: repetition trio WIP → Deprecated, hard, dim 6 out of the instrument) (2026-08-19: repetition trio demoted to WIP — construct-questionable, excluded from final analyses; 2026-08-21: five `gold` dialog-act columns added as WIP). Live dashboard:
> `python3 -c "import sys;sys.path.insert(0,'src');from
> swb_extract import registry as R;print(R.summary())"`._

> _Package split 2026-08-20: the `Extractor` column names a module in
> `src/swb_extract/features/inhouse/`. The Thomas et al. (2018) replication
> (`features/thomas2018/`: ppron, wps, wpu, wpp, boplen, poplen, pv, lv, olap, rept, repu —
> 16 ingredient columns): all 16 ingredient columns extracted + registered WIP 2026-08-22 (family `thomas`;
> the acoustic four share one pyin+RMS pass cached at `derived/thomas2018_prosody.csv`). Register columns as WIP **before** `swb-extract table`, or the loader's
> registry guard refuses the table._

## Trusted

| Column | Extractor | Family | Notes |
|---|---|---|---|
| token_count | token_count.py | volume | 0–81 tokens (the single 0 = a pure-markup utterance, correct since the angle fix), med 8, 0% null. **Laughed-word fix 2026-08-19** (audit §3 item 6b): `[laughter-yeah]` = a real spoken word, previously dropped with the non-speech brackets — 10,275 tokens in 5,714 utts (0.39% of corpus, ~20% within laughing utts, biased against high-laughter speakers). Shared `_text.tokenize` now unwraps; local duplicate tokenizers consolidated onto `_text`. Re-extraction gated EXACTLY: new − old == Laughed Word Count for all 214,204 rows, 0 violations. Same-day angle-markup fix: `<b_aside>`/`<e_aside>` (ms98 off-phone-aside delimiters, 300 tokens/147 utts) were counted as words — now dropped as markup; gated by exact recompute + delta==−angle_tokens, 0 violations |
| word_rate | word_rate.py | volume | med 2.30/s; 8 nulls BY GUARD (2026-08-19 duration guard, audit §3 6d — mistimed trans lines); laughed-word + angle fixes 2026-08-19; **kept Trusted but EXCLUDED from the final analysis (decision 2026-08-19): near-duplicate of syllable_rate (r .948 utt / .966 side) — syllable_rate is the dim-5c instrument, word_rate available as robustness alternate**; trans-span denominator — see audit §3 rate↔pause item |
| syllable_rate | syllable_rate.py | volume | med 2.92/s. Mechanism: CMUdict vowel nuclei per word (98.4% freq-weighted corpus coverage), pyphen fallback for OOV. **2026-08-19 fixes**: laughed-word unwrap (5,714 rows); per-hyphen-part lookup — whole-text path made "um-hum" OOV→pyphen guessed 1 syllable, halving the rate on 11,332 pure-backchannel utts (gated: pure um-hum new/old rate median exactly 2.00); ms98 `_N` variant suffix stripped → citation-form counts (documented approximation: 'cause counts as because=2, +0.07%); angle markup excluded. 14,316 rows changed, all recompute-verified EXACT corpus-wide (0/214,204 mismatches); 8 nulls BY GUARD (duration guard 2026-08-19, §3 6d); **the dim-5c "rate of speech" instrument for the final analysis (2026-08-19 decision; word_rate = robustness alternate)**; trans-span denominator caveat stands (articulation-rate variant = tokens/(speech frac × duration) derivable from Trusted columns) |
| loudness mean | loudness.py | volume | **WORD-TIGHT REDESIGN 2026-08-19** (audit §3 risk 6): RMS frames restricted to word-aligned intervals — slice silence no longer dilutes. Linear RMS 0–0.22, med .0116; z-score before use. Dilution quantified first (`analysis/validate_loudness_dilution.py`: old ≈ tight × speech_frac^0.67, R² .96); redesign adopted for external auditability + cross-corpus portability despite ranking robustness (old-vs-new full-corpus r(log) .905/ρ .899, new ≥ old 99.1%, median undilution ×1.457 — all matching the pre-measurement). 12 null (11 no-word-rows + 1); 81 true zeros. 9 unit tests incl. tone/silence semantics + stale-cache vintage guard (header change). Pre-redesign CSV: `utterances_v2/_archive/loudness_transspan_pre_redesign_2026-08-19.csv`. Standing caveat: between-caller level confounded with telephone line gain — loudness-as-recorded, not vocal effort |
| loudness std | loudness.py | volume | word-tight 2026-08-19: RMS spread *within speech* (old value was inflated by speech/silence alternation); med .0090 |
| loudness range | loudness.py | volume | word-tight 2026-08-19: max−min over speech frames (old min was the silence floor ⇒ range ≈ peak); med .0374 — now a genuine within-speech dynamic-range measure for Tannen dim 2a "marked amplitude shifts" |
| loudness speech frac | loudness.py | volume | NEW 2026-08-19: clipped word time / slice duration (med .623) — dilution diagnostic for portability audits on future corpora; NOT an analysis variable (pausing arithmetic by construction) |
| Personal Pronouns per Second | pronoun_per_second.py | tannen | **v2 REDESIGN 2026-08-19 (same-day reversal of the keep-as-is call, on decision)**: 1st/2nd-person closed-list count / duration — THE dim-1 lexical instrument (Chafe involvement marker). List (16, = PERSONAL_PRONOUNS in the extractor): i, me, my, mine, myself, we, us, our, ours, ourselves, you, your, yours, yourself, yourselves, y'all. No tagger: closed class, deterministic, corpus-portable; spaCy removed from the trusted line (v1 spaCy-PRON was ~50% non-personal — total pronominalization; archived `_archive/pronoun_per_second_v1_spacy_PRON_2026-08-19.csv`). Gates: corpus personal/total ratio .496 (sample predicted .501); 8 guard nulls; 575 rows where the list catches lowercase-tagging misses. Zero-inflated (med 0 — backchannels) — pool at caller level. Contractions match by base (i'm→i); y'all whole; partial attempts (i[t]-) excluded. Empath decision resolved 2026-09-07: pooled hits NOT needed — this column is the dim-1 parcel alone (SINGLE block); laughed-word fix 2026-08-19 (2,035 rows changed, confined to laughed-word utts); 8 nulls by the 2026-08-19 duration guard (§3 6d) |
| Filled Pauses per Second | filled_pause_per_second.py | volume | **NEW 2026-08-20 — the hesitation half of the filler split** (§4E-c): count of {um, uh, er} / guarded trans-span duration. Unambiguous by construction — um/uh have no lexical homographs, so this column carries NONE of the so/like/well conflation. Gates: split pair sums to the legacy combined column on all 214,204 rows (0 violations; 210,998 bit-exact + 3,198 within 1e-12 rel — float assoc. only), null sets identical (the 8 §3-6d guard rows). 74.6% zero (zero-inflated — pool at caller level), nonzero med .206/s. "er" is a dead list entry (0 hits corpus-wide) |
| Discourse Markers per Second | discourse_marker_per_second.py | volume | **NEW 2026-08-20 — the discourse-marker half of the filler split** (§4E-c; Treebank `{D}` class — no HI/HC sign assumed, see tannen_feature_map row 12): count of {so, well, like, you know, i mean, i guess, basically} / guarded duration. Same gates as Filled Pauses (exact-sum identity, null sets). 68.1% zero, nonzero med .223/s. CONSTRUCT CAVEAT: so/like/well are homograph-conflated (lexical uses count — 34.5% of combined-column hits); Treebank {D}/{C}/{F} gold study pending (`corpus/annotations/treebank/dysfl/`, 36 convs, 0.8 precision bar) — tokens that fail may be excised, which breaks the sum identity with the legacy column (re-gate on any list change) |
| Filler Words per Second | filler_word_per_second.py | volume | **SUPERSEDED for analysis 2026-08-20 by the split pair above** (kept for replication + the exact-sum gate; = Filled Pauses + Discourse Markers by construction — never use alongside them, it's their sum). **AUDITED FAITHFUL 2026-08-19** (interactional walkthrough): full-corpus recompute vs shipped CSV 0/214,204 mismatches; the 8 nulls are exactly the §3-6d duration-guard rows; phrase matcher hand-checked on edge cases (greedy longest-match is safe — no 1-token filler prefixes a 2-token phrase in this list). allowlist verified vs docstring (um/uh/er + so/well/like/you know/i mean/i guess/basically, phrase-aware); hit composition measured 2026-08-19: 183,496 hits = **41.8% filled pauses** (uh 32.0%, um 9.7%, er **0 — dead list entry**) vs **58.2% discourse markers** (you know 17.2%, so 12.8%, like 11.0%, well 10.7%, i mean 3.6%, i guess 2.6%, basically 0.4%) — the §4E-c cancellation risk is live and the discourse-marker side dominates; so/like/well (34.5% of hits) are homograph-conflated (lexical "i like dogs"/"so we went"/"well water" count as fillers — construct, not computation); laughed-word fix 2026-08-19 (792 rows changed, confined — a laughed "um" now counts); trans-span denominator (§3 rate↔pause item) |
| FTO Sec | fto.py | interactional | floor-transfer offset; 64.5% null BY DESIGN (floor transfers only); med +0.14s, matches Heldner & Edlund (audit §3.1) |
| Onset Gap Sec | fto.py | interactional | 25.4% null = backchannels + conversation-initial turns, decomposed (NB07 Step 11); rides tests/test_fto.py |
| Turn Initial Flag | fto.py | interactional | FTO defined ⇒ flag=1 verified corpus-wide (NB07 Step 11); rides tests/test_fto.py |
| Backchannel Flag | fto.py | interactional | lexical allowlist — corpus-wide agreement with the notebook is_bc verified (NB07 Step 11); allowlist measured vs NXT gold (Step 12): P .842 / R .917 on primary {b}, F1 .878; DAMSL-trained classifier (Step 15) CV F1 .888 admitted as an alternative bc_def (in-notebook mask, not a column) |
| Interjection Flag | fto.py | interactional | contained other-speaker utterance (no floor transfer); rides tests/test_fto.py; lexical-allowlist heuristic — NXT gold P/R pending (§4C12) |
| Latching Flag | latching_flag.py | interactional | Latching=1 ⇒ raw FTO ∈ [0, 0.2] s verified corpus-wide at 0 violations AFTER Step 11 caught a stale pre-Jun-26-FTO vintage (1,333/214,204 rows corrected on re-extraction 2026-07-09); 17.9% of defined transfers latched; LATCH_MAX_SEC=0.2 documented + sweepable |
| Overlap Duration Sec | overlap.py | interactional | word-level intervals; cooperative/obstructive split = the overlap_split extractor (§4E-a, landed T6) |
| Cooperative Overlap Count | overlap_split.py | interactional | coop/obstr split on merged FTO turns (§4E-a, Delta 7d); W=1.0s fixed in advance; bc-only overlaps cooperative by definition; PROMOTED on NB07 Step 14 gold checks (2026-07-09): gold-`b` overlap events 98.1% cooperative (bar 90); `+`-continuation floor retention across intervening talk 73.2% (bar 70) |
| Obstructive Overlap Count | overlap_split.py | interactional | floor-taking overlap where the holder ceded within W=1.0s of onset ("successful interruption"); 31.7% of the 74,550 corpus-wide overlap events; caller `obstructive_overlap_share` = the involvement-panel variable (Step 16); see Cooperative Overlap Count |
| Overlap Count | overlap.py | interactional | 0% null among placeable; verified vs negative-FTO coherence (NB07 Step 11) |
| Overlap Onset Flag | overlap.py | interactional | onset-in-overlap coheres with FTO<0 at transfers (NB07 Step 11) |
| Within Pause Total Sec | within_utterance_pauses.py | interactional | sums ALL positive inter-word gaps (Count only ≥0.25 s — so Count=0 does NOT imply Total=0); invariants verified corpus-wide (NB07 Step 11); zero-inflated |
| Within Pause Count | within_utterance_pauses.py | interactional | gaps ≥ PAUSE_MIN_SEC=0.25; Count≥1 ⇔ Max≥0.25 verified (NB07 Step 11) |
| Within Pause Rate | within_utterance_pauses.py | interactional | Total/span; ≤1 verified corpus-wide (NB07 Step 11) |
| Max Within Pause Sec | within_utterance_pauses.py | interactional | ≤ Total verified corpus-wide (NB07 Step 11) |
| Rising Terminal Flag | rising_terminal.py | interactional | **kept Trusted but EXCLUDED from the NB08 parcel instrument/score (decision 2026-09-06)** — exclusion, not deprecation (gold pass below stands): grounds = the MNAR missingness structure + the weakest Tannen anchor in the construct map (a dims-3d/4 bridge, ours); resolves the frozen table's ⚑; the question family routes to the gold_acts columns (WIP) instead; RT stays in the 26-feature appendix CPCA as recorded. 30.3% null MNAR, voicing/length-dependent — null rate RISES with utterance length (21%→35% for ≤2→≥6 tokens; NB07 Step 11), contra the docstring's short-tails guess; do NOT fillna(0) — treat null as indeterminate. **2026-07-29 semitone redesign:** flag now thresholds the semitone slope at 3.07 st/s = the old 30 Hz/s operating point at the corpus-median F0 (169.0 Hz) — the Hz rule demanded ~1.7× steeper proportional rises from low-pitched speakers (side-level r(share, pitch mean) = +.485 under Hz); window/anchor/voiced gate unchanged, null set identical. Gold-DA known-groups (`analysis/validate_rising_terminal.py`): rising share .616 yn-questions vs .248 statements (2.48×, AUC .721) PASS; declarative-q `^d` .410 (1.65×, p 2.5e-08 — first-run 2× arm failed, adjudicated on record). Caller level: pooled defined-only share over ALL panel callers (tails min 2 / median 154) + `rt_defined_share` coverage companion; the ≥30-tails subset (428/487) is the robustness re-run, not eligibility |
| Terminal F0 Slope | rising_terminal.py | interactional | clip at ±1000 Hz/s before use (bound chosen + recorded, NB07 Step 11); unchanged by the 2026-07-29 redesign (same fit, Hz/s continuity column) |
| Terminal ST Slope | rising_terminal.py | interactional | semitone-scale terminal slope (st/s), 12·log2(F0) fit over the same voiced tail frames as Terminal F0 Slope — the pitch-level-invariant contour the flag thresholds (2026-07-29 redesign); same 30.3% null set; do NOT fillna(0); excluded from the NB08 instrument with the flag (2026-09-06 — see Rising Terminal Flag row) |
| Laughter Count | laughter.py | interactional | full-corpus bracket reconciliation vs Transcript exact (NB07 Step 11); RATE-normalize at analysis — raw counts are a talkativeness proxy (Jun-19 audit) |
| Laughed Word Count | laughter.py | interactional | reconciled corpus-wide (NB07 Step 11) |
| Noise Count | laughter.py | interactional | counted before stripping (audit §3.5) |
| Vocalized Noise Count | laughter.py | interactional | counted before stripping (audit §3.5) |
| Other Bracket Count | laughter.py | interactional | 255 corpus-wide — negligible |
| Personal Hits | personal_focus_score.py | tannen | **EXCLUDED from the NB08 instrument with its two siblings (decision 2026-09-07)** — PF_ratio dropped from the dim-1 parcel (Empath mapping unvalidated, §4C12; the "still needed for dim 1?" open decision resolved no); dim 1 = Personal Pronouns per Second v2 alone; columns stay Trusted (exclusion, not deprecation). raw ingredient — pool-then-ratio at unit level ONLY (coverage + non-degeneracy evidence NB07 Step 11); laughed-word fix 2026-08-19 (5,714 rows changed across the 3 ingredient columns, confined) |
| Impersonal Hits | personal_focus_score.py | tannen | raw ingredient for pooling (see Personal Hits note) |
| Analyzed Tokens | personal_focus_score.py | tannen | density basis for pooling |
| pitch mean | pitch.py | prosody | 8% null (unvoiced); 50–400 Hz clamp; static moment only |
| pitch std | pitch.py | prosody | 8% null |
| pitch range | pitch.py | prosody | 8% null |
| Gender | demographics.py | meta | corpus covariate (114k F / 99k M) |
| Region | demographics.py | meta | corpus covariate (9 regions) |
| Year Born | demographics.py | meta | corpus covariate (1924–1975) |
| Generation | demographics.py | meta | derived from Year Born |
| Decade | demographics.py | meta | derived from Year Born |
| Education | demographics.py | meta | corpus covariate (0–9) |

## WIP

| Column | Extractor | Family | Notes |
|---|---|---|---|
| Question Flag | question_flags.py | interactional | VALIDATED vs NXT gold 2026-07-09 (NB07 Step 12, 52,890 labelled utts): precision .553 / recall .236 (recall by type: syntactic 29%, declarative 6%, tag 3%); gold q-rate 7.83% vs flag 3.34% — the audit's rate-gap explained; EXCLUDED from analyses per the pre-registered 0.8 bar; Tier-3 marker-skip fix declined on evidence (+716 gain vs +471 new FPs); classifier route CLOSED 2026-07-09 (NB07 Step 15): question clf CV F1 .681 < the .70 bar → not admitted; no question_rate anywhere, gold 7.83% quoted descriptively (§4C12) |
| Echo Question Flag | question_flags.py | interactional | adjudicated 2026-07-09 vs gold `bh` (backchannel-in-question-form): precision .209 / recall .025 — construct mismatch; keep out of analyses; gold `bh` itself is the panel variable (T9) |
| Machine Gun Question Score | machine_gun_question.py | interactional | 49.7% null — decomposition pending (audit §3.4 / C1 Tier 4); ingredients gated on T4; FTO-derived → same stale-vintage risk Step 11 caught in latching — RE-EXTRACT before first use |
| Machine Gun Question Flag | machine_gun_question.py | interactional | 49.7% null |
| Mutual Revelation Flag | mutual_revelation_flag.py | tannen | EXCLUDED from the paper — spot-checked precision ~30–40% (Jun-19 audit); no gold rescue exists; keep out until hand-labeled |
| Topic Label | topic_label.py | meta | assigned SWBD topic; join-validity unchecked, never yet joined (audit §2.6/§4D16 — out of submission scope) |
| Gold Act | gold_acts.py | gold | **NEW 2026-08-21 — human-labelled dialog act (SWBD-DAMSL) per utterance, terminal act.** Two joins of the same annotation to ms98 utterances: NXT time join (544 convs, 52,890 utts) + SwDA text join (`corpus/swda/`, 458 further convs, 45,820 utts; `swda.align_side`, difflib word-sequence alignment, per-conversation A/B channel-swap detection — 82/1,002 SwDA convs are swapped). Blank outside the 1,001 labelled conversations (46.1% of rows labelled). Text join validated on the 544-conv overlap (`analysis/validate_gold_acts.py`, bars stated first): question-flag agreement **.9978** on 49,034 utts, information .9983, echo .9999; coverage .981 of the time join; side match ratio median .958, 17/1,088 sides < .85 blanked. These are labels (SWBD-DAMSL inter-labeler κ ≈ .80), not a computed feature — parse with `nxt.parse_tag`; NXT spells `%-` where SwDA has `%` |
| Gold DA Source | gold_acts.py | gold | nxt / swda — question rate 6.18% vs 5.92% by source; keep as a covariate until a source effect is ruled out at caller level |
| Gold Question Flag | gold_acts.py | gold | **Tannen 3d** (use of questions): terminal base ∈ {qy, qw, qo, qh, qr, qrr} or tag `^g`; 5,981 = 6.06% of labelled utts; dense at side level (87% of sides ≥1). Replaces the excluded heuristic Question Flag (P .553) for analyses on the gold frame |
| Gold Information Question Flag | gold_acts.py | gold | **Tannen 4b**: qy / qw / qo / qr / qrr incl. `^d` declarative forms; excludes rhetorical qh and bh; 5,708 = 5.78% |
| Gold Echo Question Flag | gold_acts.py | gold | **Tannen 4a** (echo questions as back-channel): `bh` ("oh really?", "is that right?") or a question carrying the mirror mark `^m`; 674 = 0.68% — SPARSE: corpus-level contrasts or shrunken caller rates, never raw per-caller ratios |
| wpu | thomas2018/wpu.py | thomas | **NEW 2026-08-22 — Thomas et al. (2018) replication, ingredient column** (package docstring `features/thomas2018/__init__.py`; per-utterance ingredients, pooled per side by each module's `aggregate()`): word count per utterance (shared tokenizer). Paper's wpu = mean per side; their largest involvement loading (+.45). 0 nulls |
| ppron | thomas2018/ppron.py | thomas | 1st/2nd-person pronoun COUNT per utterance (the in-house 16-form list). Paper's ppron = Σppron / Σwpu per side, i.e. per word (LIWC convention); loading +.13 |
| wps sec | thomas2018/wps.py | thomas | word-tight utterance duration (s) — first-word onset to last-word offset. Paper's wps = Σwpu / Σ(wps sec) per side, a micro-average (NOT the mean of per-utterance rates); loading +.39; blank where no word rows |
| olap | thomas2018/olap.py | thomas | 1 if the utterance begins while the partner is still talking (word-tight bounds; every transcript line counts, backchannels included, as the paper notes). Paper's olap = mean per side; loading −.01, the paper's one sign reversal. corpus-wide side median .36 (IQR .24–.49); listener backchannel lines are the bulk of the overlapping starts |
| poplen | thomas2018/poplen.py | thomas | post-other pause (s): partner's running end → this utterance's start, defined only for the partner's NEXT utterance; blank in overlap, after own talk, or before the partner has spoken. Paper's poplen = mean per side; loading −.27 |
| rept | thomas2018/rept.py | thomas | distinct stemmed terms repeated from the speaker's OWN previous line (frozen NLTK stopword list + Porter; fillers exactly {um, uh, uh-huh} as in the paper — Switchboard's "um-hum" therefore counts, sweepable `_terms.FILLERS`); blank for a side's first utterance. Paper's rept = mean; loading +.39. SELF-repetition ("persistence"), not Tannen dim 6 |
| repu | thomas2018/repu.py | thomas | 1 if rept ≥ 1, else 0; blank likewise. Paper's repu = mean; loading +.39 |
| wpp pauses | thomas2018/wpp.py | thomas | between-own pause COUNT per utterance = maximal unvoiced runs bounded by voiced frames inside the slice (pyin, pitch.py settings; the paper's "periods where OpenSMILE reports no F0", taken literally — consonant stretches included, as in the paper). Paper's wpp = Σwpu / Σ(wpp pauses) per side; loading +.44 — but **.07 on Switchboard**: wpp and boplen share this denominator (raw r +.83 at side level), an arithmetic coupling the paper's α/PCA inherit. Utterance med 3 pauses; side med 2.0 words/pause (MISC .84). Identical to `boplen n` by construction |
| boplen n | thomas2018/boplen.py | thomas | same pause count (self-describing sibling of `boplen sec`) |
| boplen sec | thomas2018/boplen.py | thomas | total between-own pause seconds per utterance. Paper's boplen = Σsec / Σn per side; loading −.10 (Switchboard PC1 +.09, sign flip, ≈ 0 both). Utterance med .53 s of pause; side med .20 s (MISC .105 — ms98 lines are longer and more pause-rich); the paper's "order of 0.1 s" is the same literal definition |
| pv n | thomas2018/pv.py | thomas | voiced frames in the slice (pyin, 64 ms window / 16 ms hop at 8 kHz); the pooling weight for pv/lv. Extracted 2026-08-22 (266.9 h audio, one shared pass): 0 nulls; 8.0% of utterances have 0 voiced frames (= pitch.py's 8% unvoiced) |
| pv mean | thomas2018/pv.py | thomas | mean F0 (Hz) over voiced frames; med 176 Hz |
| pv var | thomas2018/pv.py | thomas | F0 variance (Hz², population) over voiced frames. Paper's pv = variance of ALL the side's voiced frames pooled (N = Σn, μ = Σnμᵢ/N, Var = Σn(vᵢ+μᵢ²)/N − μ²), Hz-scale as in the paper — pitch-level-dependent; loading +.09. Utterance med 513 Hz², side-level pooled med 1,571 Hz² (MISC through the same code: 3,712 — OpenSMILE SHS octave errors); Switchboard PC1 weight −.04 (paper +.09) |
| lv n | thomas2018/lv.py | thomas | voiced frames (= pv n; carried so the CSV pools alone) |
| lv mean | thomas2018/lv.py | thomas | mean frame RMS over voiced frames (the in-house loudness measure; OpenSMILE loudness not a dependency); med .020 |
| lv var | thomas2018/lv.py | thomas | RMS variance over voiced frames; paper's lv pooled as pv; loading +.21 (Switchboard PC1 +.11); utterance med 1.9e-4; telephone-line-gain caveat carried |

## Deprecated (replication only)

| Column | Extractor | Family | Notes |
|---|---|---|---|
| Repetitions In Current Utterance | repetitions_in_current.py | volume | **MAYBE for final analysis (2026-08-19 audit decision)**: raw pair count is ~75% length — r(pairs, token_count) .80 utt / .88 side (log), 72.5% of pairs from function/filler words; genuinely stylistic residue exists only in the normalized form pairs/C(tokens,2) (side med 1.7%, IQR 1.5–2.0%) — if used in the final set, use normalized or log1p, never raw next to token_count; NOT a dim-6 instrument (Step 13: 24.5% repair material, r≈0 with gold mirror — dim 6 = gold ^m rate). Faithful: full recompute 0/214,204 mismatches. pair count Σ C(n,2) BY DESIGN — max 109 > max token_count 81 is the quadratic tail (boundary test pins C(15,2)=105); full-corpus recompute reconciled (NB07 Step 11); laughed-word fix 2026-08-19 (1,721 rows changed, confined); heavy right tail — consider log1p at analysis time (audit §2.8). Gold de-conflation DONE (NB07 Step 13): 24.5% of pair-repetitions attributable to gold repair material (de-conflated variant computed, 544-conv subset; extractor↔gold r=.978); ≈0 side-level correlation with gold mirror `^m` (sparse ~0.5/side) — self-repetition ≠ allo-repetition; involvement-relevant repetition = the gold mirror rate (T9); **DEMOTED Trusted→WIP 2026-08-19 (family decision): computationally faithful but construct-questionable — excluded from final analyses; kept for replication; future direction = MSFT-paper-style repetition/entrainment redesign (see audit §4E-h)** **DEPRECATED 2026-09-12 (hard, by decision): dim 6 has no automatic instrument in NB08 — 9 parcels; replication only** |
| Repetitions In Previous Utterance | repetitions_in_previous.py | volume | **DOUBTFUL for final analysis (2026-08-19 audit)**: 31.8% of predecessors are the SAME speaker; 76% of matches are function/filler words; side-level r(log matches, log token-product) .934 — the raw column is talkativeness-product; DIRECT gold disqualification as dim 6b: side-level r with gold ^m mirror .10 raw / .19 normalized-other-speaker (1,284 gold sides). Faithful (recompute 0/214,204). Echo-detector replacement route CLOSED same day (`analysis/validate_echo_detector.py`: gold mirrors not lexically recoverable — recall ceiling 41%, best F1 .208 vs the .8 bar) — dim 6 = gold ^m rate on the 642-conv subset. cross-utterance pair matches vs chronological predecessor; 0.9% null = conversation-initial, verified ≤1/conversation (NB07 Step 11); same tail caveat; gold de-conflation (Step 13): 14.2% repair-attributable (time-ordered-predecessor approximation); laughed-word fix 2026-08-19 (1,546 rows changed, confined to laughed utts + their chronological successors); **DEMOTED Trusted→WIP 2026-08-19: legacy lineage = shared chronological-predecessor pointer with the superseded Turn Gap (lemmatization dropped in rewrite); retained as corrected-replication artifact only; excluded from final analyses; MSFT-paper redesign direction (§4E-h)** **DEPRECATED 2026-09-12 (hard, by decision): dim 6 has no automatic instrument in NB08 — 9 parcels; replication only** |
| Repetitions per Second | repetition_per_second.py | volume | numerator = repetition_rate count (unique words reaching ≥2), NOT the pair count — by design per docstring; shares tokenize with the repetition family (NB07 Step 11); laughed-word fix 2026-08-19 (1,356 rows changed, confined); 8 nulls by the 2026-08-19 duration guard (§3 6d); **DEMOTED Trusted→WIP 2026-08-19: audit SKIPPED by decision — numerator is within-utterance repetition, inherits the family construct verdict; excluded from final analyses; MSFT-paper redesign direction (§4E-h)** **DEPRECATED 2026-09-12 (hard, by decision): dim 6 has no automatic instrument in NB08 — 9 parcels; replication only** |
| Turn Gap | turn_gap.py (dissolved 2026-07-31 → shared helpers in _turn_index.py; module in git history) | interactional | legacy gap vs chronological predecessor incl. backchannels; 60% negative; superseded by FTO Sec |
| Pronoun Rate | pronoun_rate.py (dissolved 2026-07-31 → shared helpers in _text.py; module in git history) | volume | per-token variant; using Pronouns per Second instead |
| Repetition Rate | repetition_rate.py (dissolved 2026-07-31 → shared helpers in _text.py; module in git history) | volume | per-token variant; using Repetitions per Second instead |
| Filler Word Rate | filler_word_rate.py (dissolved 2026-07-31 → shared helpers in _text.py; module in git history) | volume | per-token variant; using Filler Words per Second instead |
| Personal Focus Score | personal_focus_score.py | tannen | DEGENERATE per-utterance ratio — 71.5% null, saturates 0/1 (Jun-19 audit 2/10); pool the raw hit columns instead (NB07 Step 11; audit §3.3) |
